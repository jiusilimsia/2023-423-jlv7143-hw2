# Clouds

## Build the Docker image

```bash
docker build ...
```

## Run the entire model pipeline

```bash
docker run ...
```

## Build the Docker image for tests

```bash
docker build ...
```

### Run the tests

```bash
docker run ...
```
